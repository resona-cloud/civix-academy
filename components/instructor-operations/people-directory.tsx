import Link from "next/link";
import { mockCohorts, mockLearnerProfiles } from "@/lib/instructor-operations/mock-data";

export function PeopleDirectory() {
  return <><header className="mb-8"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-indigo-700">Learners</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">People</h1><p className="mt-2 text-slate-600">Review learner assignments, progress, performance, and operational risk.</p></header><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{mockLearnerProfiles.map((learner) => { const cohort = mockCohorts.find((item) => item.id === learner.cohort_id); const course = learner.course_progress[0]; return <article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" key={learner.id}><div className="flex h-11 w-11 items-center justify-center rounded-full bg-indigo-100 font-semibold text-indigo-800">{learner.display_name.split(" ").map((part) => part[0]).join("")}</div><h2 className="mt-4 text-lg font-semibold">{learner.display_name}</h2><p className="mt-1 text-sm text-slate-500">{learner.classification}</p><p className="mt-3 text-xs text-slate-400">{cohort?.name}</p><div className="mt-4 flex justify-between text-xs text-slate-500"><span>Course progress</span><span>{course?.progress_percent ?? 0}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-indigo-600" style={{ width: `${course?.progress_percent ?? 0}%` }} /></div><div className="mt-4 flex justify-between text-xs"><span className="text-slate-500">{learner.risk_flags.length} risk flag{learner.risk_flags.length === 1 ? "" : "s"}</span><Link className="font-semibold text-indigo-700" href={`/people/${learner.id}`}>Open profile -&gt;</Link></div></article>; })}</div></>;
}
