import type { CertificationProgress, CertificationTrack } from "@/lib/certifications/types";

export function CertificationProgressView({ certification, progress }: { certification: CertificationTrack; progress: CertificationProgress }) {
  return <section className="rounded-xl border border-slate-200 bg-white p-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-wider text-sky-700">Certification progress</p><h2 className="mt-1 text-xl font-semibold capitalize">{progress.eligibility_status.replaceAll("_", " ")}</h2></div><strong className="text-3xl">{progress.progress_percent}%</strong></div><div className="mt-4 h-2.5 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-sky-600" style={{ width: `${progress.progress_percent}%` }} /></div><div className="mt-6 grid gap-3">{certification.requirements.map((requirement) => { const item = progress.requirements.find((candidate) => candidate.requirement_id === requirement.id); return <div className="flex flex-wrap items-center justify-between gap-3 rounded-lg bg-slate-50 px-4 py-3" key={requirement.id}><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{requirement.requirement_type}</p><p className="mt-1 text-sm font-medium">{requirement.title}</p></div><div className="text-right"><p className="text-sm font-medium capitalize">{item?.status.replaceAll("_", " ") ?? "not started"}</p>{requirement.minimum_score !== null ? <p className="text-xs text-slate-500">Minimum {requirement.minimum_score}%{item?.score !== null && item?.score !== undefined ? ` - Score ${item.score}%` : ""}</p> : null}</div></div>; })}</div></section>;
}
