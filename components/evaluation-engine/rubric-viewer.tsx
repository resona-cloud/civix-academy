import type { Competency, Rubric } from "@/lib/evaluation-engine/types";

export function RubricViewer({ rubric, competencies }: { rubric: Rubric; competencies: Competency[] }) {
  return <section className="rounded-xl border border-slate-200 bg-white p-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wider text-emerald-700">Evaluation criteria</p><h2 className="mt-1 text-xl font-semibold">{rubric.title}</h2></div><span className="text-sm font-medium text-slate-600">{rubric.passing_score}% to pass</span></div><p className="mt-2 text-sm text-slate-500">{rubric.description}</p><div className="mt-5 grid gap-3">{rubric.criteria.map((criterion) => { const competency = competencies.find((item) => item.id === criterion.competency_id); return <article className="rounded-lg border border-slate-200 p-4" key={criterion.id}><div className="flex items-start justify-between gap-4"><div><h3 className="font-semibold">{criterion.title}</h3><p className="mt-1 text-sm leading-6 text-slate-600">{criterion.description}</p>{competency ? <p className="mt-2 text-xs text-emerald-700">Competency: {competency.name}</p> : null}</div><div className="text-right text-xs text-slate-500"><strong className="block text-base text-slate-800">{criterion.max_score}</strong>{criterion.weight_percent}% weight</div></div></article>; })}</div></section>;
}
